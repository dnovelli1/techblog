<div class="comments">
    {{#each this}}
    <div class="row">
        <div class="col s12 m6">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <p class="creator">Comment by: {{user.username}}</p>
                    <p>Created at: {{created_at}}</p>
                </div>
                <div class="card-action">
                    <p>{{body}}</p>
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</div>